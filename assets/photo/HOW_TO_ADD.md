Drop your headshot in this folder named exactly:

    photo.jpg   (or photo.jpeg / photo.png / photo.webp)

The page automatically detects it on load — no code changes needed.
If no file is found, the hero shows an "AE" placeholder instead.
